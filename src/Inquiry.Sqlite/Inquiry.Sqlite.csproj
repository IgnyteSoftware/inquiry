<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net8.0;net9.0;net10.0</TargetFrameworks>
    <Description>SQLite provider for Inquiry. Bundles the SQLite-flavoured source generator (Inquiry.Sqlite.Analyzer) so referencing this package alone enables store SQL generation.</Description>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <!-- Runs the trim/AOT analyzers and stamps the trimming/AOT-compatibility metadata. -->
    <IsAotCompatible>true</IsAotCompatible>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Inquiry\Inquiry.csproj" />
    <!-- Build the analyzer + its shared framework dep so the bin paths below exist before pack. -->
    <ProjectReference Include="..\Inquiry.Sqlite.Analyzer\Inquiry.Sqlite.Analyzer.csproj" ReferenceOutputAssembly="false" PrivateAssets="all" />
    <PackageReference Include="Microsoft.Data.Sqlite" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Inquiry.Sqlite.Tests" />
  </ItemGroup>

  <!-- Resolve analyzer DLL paths dynamically so dotnet pack works under UseArtifactsOutput or custom OutputPath. -->
  <Target Name="_ResolveAnalyzerDlls" BeforeTargets="_GetPackageFiles">
    <MSBuild Projects="..\Inquiry.Sqlite.Analyzer\Inquiry.Sqlite.Analyzer.csproj"
             Targets="GetTargetPath"
             Properties="Configuration=$(Configuration)">
      <Output TaskParameter="TargetOutputs" ItemName="_AnalyzerTargetPath" />
    </MSBuild>
    <ItemGroup>
      <None Include="@(_AnalyzerTargetPath)" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
      <None Include="$([System.IO.Path]::GetDirectoryName('%(_AnalyzerTargetPath.Identity)'))\Inquiry.Generators.Shared.dll"
            Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
    </ItemGroup>
  </Target>
</Project>
