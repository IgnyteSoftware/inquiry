# SQLite Query Plans

Generated by `dotnet run --project benchmarks/Inquiry.Benchmarks -c Release -- --capture-plans`.
Each section shows `EXPLAIN QUERY PLAN` output for the SQL statements used in the benchmark suite.

## ShipperCrud

### SelectAll

```sql
SELECT ShipperID, CompanyName, Phone FROM Shippers
```

```
SCAN Shippers
```

### SelectByKey

```sql
SELECT ShipperID, CompanyName, Phone FROM Shippers WHERE ShipperID = $id
```

```
SEARCH Shippers USING INTEGER PRIMARY KEY (rowid=?)
```

### SelectByField

```sql
SELECT ShipperID, CompanyName, Phone FROM Shippers WHERE CompanyName = $c
```

```
SCAN Shippers
```

### Insert

```sql
INSERT INTO Shippers (CompanyName, Phone) VALUES ($company, $phone)
```

```
```

### Update

```sql
UPDATE Shippers SET CompanyName = $company, Phone = $phone WHERE ShipperID = $id
```

```
SEARCH Shippers USING INTEGER PRIMARY KEY (rowid=?)
```

### Upsert

```sql
INSERT INTO Shippers (ShipperID, CompanyName, Phone) VALUES ($id, $company, $phone) ON CONFLICT(ShipperID) DO UPDATE SET CompanyName = excluded.CompanyName, Phone = excluded.Phone
```

```
```

## CustomerCrud

### SelectAll

```sql
SELECT CustomerID, CompanyName, ContactName, ContactTitle, Address, City, Region, PostalCode, Country, Phone, Fax FROM Customers
```

```
SCAN Customers
```

### SelectByKey

```sql
SELECT CustomerID, CompanyName, ContactName, ContactTitle, Address, City, Region, PostalCode, Country, Phone, Fax FROM Customers WHERE CustomerID = $id
```

```
SEARCH Customers USING INDEX sqlite_autoindex_Customers_1 (CustomerID=?)
```

### SelectByField

```sql
SELECT CustomerID, CompanyName, ContactName, ContactTitle, Address, City, Region, PostalCode, Country, Phone, Fax FROM Customers WHERE Country = $c
```

```
SCAN Customers
```

### Insert

```sql
INSERT INTO Customers (CustomerID, CompanyName, ContactName, Country, City) VALUES ($id, $company, $contact, $country, $city)
```

```
```

### Update

```sql
UPDATE Customers SET CompanyName = $company, ContactName = $contact, Country = $country, City = $city WHERE CustomerID = $id
```

```
SEARCH Customers USING INDEX sqlite_autoindex_Customers_1 (CustomerID=?)
```

### Upsert

```sql
INSERT INTO Customers (CustomerID, CompanyName, ContactName, Country, City) VALUES ($id, $company, $contact, $country, $city) ON CONFLICT(CustomerID) DO UPDATE SET CompanyName = excluded.CompanyName, ContactName = excluded.ContactName, Country = excluded.Country, City = excluded.City
```

```
```

## ProductCrud

### SelectAll

```sql
SELECT ProductID, ProductName, SupplierID, CategoryID, QuantityPerUnit, UnitPrice, UnitsInStock, UnitsOnOrder, ReorderLevel, Discontinued FROM Products
```

```
SCAN Products
```

### SelectByKey

```sql
SELECT ProductID, ProductName, SupplierID, CategoryID, QuantityPerUnit, UnitPrice, UnitsInStock, UnitsOnOrder, ReorderLevel, Discontinued FROM Products WHERE ProductID = $id
```

```
SEARCH Products USING INTEGER PRIMARY KEY (rowid=?)
```

## Pagination

### OffsetPage

```sql
SELECT ProductID, ProductName, SupplierID, CategoryID, QuantityPerUnit, UnitPrice, UnitsInStock, UnitsOnOrder, ReorderLevel, Discontinued FROM Products ORDER BY ProductID LIMIT $limit OFFSET $off
```

```
SCAN Products
```

### KeysetPage

```sql
SELECT ProductID, ProductName, SupplierID, CategoryID, QuantityPerUnit, UnitPrice, UnitsInStock, UnitsOnOrder, ReorderLevel, Discontinued FROM Products WHERE ProductID > $after ORDER BY ProductID LIMIT $limit
```

```
SEARCH Products USING INTEGER PRIMARY KEY (rowid>?)
```

## Predicate

### Search

```sql
SELECT ProductID, ProductName, SupplierID, CategoryID, QuantityPerUnit, UnitPrice, UnitsInStock, UnitsOnOrder, ReorderLevel, Discontinued FROM Products WHERE UnitPrice >= $min AND ProductName LIKE $pattern
```

```
SCAN Products
```

### InList (3 elements)

```sql
SELECT ProductID, ProductName, SupplierID, CategoryID, QuantityPerUnit, UnitPrice, UnitsInStock, UnitsOnOrder, ReorderLevel, Discontinued FROM Products WHERE CategoryID IN ($c0, $c1, $c2)
```

```
SEARCH Products USING INDEX IX_Products_CategoryID (CategoryID=?)
```

## Aggregate

### Projection

```sql
SELECT ProductID, ProductName, UnitPrice FROM Products
```

```
SCAN Products
```

### Count

```sql
SELECT COUNT(*) FROM Products
```

```
SCAN Products USING COVERING INDEX IX_Products_SupplierID
```

### Sum

```sql
SELECT SUM(UnitPrice) FROM Products
```

```
SCAN Products
```

### Avg

```sql
SELECT AVG(UnitPrice) FROM Products
```

```
SCAN Products
```

### Min

```sql
SELECT MIN(UnitPrice) FROM Products
```

```
SEARCH Products
```

### Max

```sql
SELECT MAX(UnitPrice) FROM Products
```

```
SEARCH Products
```

## EagerLoading

### Categories (related query)

```sql
SELECT CategoryID, CategoryName, Description FROM Categories
```

```
SCAN Categories
```

## Batch

### Insert Region

```sql
INSERT INTO Region (RegionID, RegionDescription) VALUES ($id, $desc)
```

```
```

## InCardinality

### IN 10 elements

```sql
SELECT ProductID, ProductName, SupplierID, CategoryID, QuantityPerUnit, UnitPrice, UnitsInStock, UnitsOnOrder, ReorderLevel, Discontinued FROM Products WHERE CategoryID IN ($c0,$c1,$c2,$c3,$c4,$c5,$c6,$c7,$c8,$c9)
```

```
SEARCH Products USING INDEX IX_Products_CategoryID (CategoryID=?)
```

### IN 100 elements

```sql
SELECT ProductID, ProductName, SupplierID, CategoryID, QuantityPerUnit, UnitPrice, UnitsInStock, UnitsOnOrder, ReorderLevel, Discontinued FROM Products WHERE CategoryID IN ($c0,$c1,$c2,$c3,$c4,$c5,$c6,$c7,$c8,$c9,$c10,$c11,$c12,$c13,$c14,$c15,$c16,$c17,$c18,$c19,$c20,$c21,$c22,$c23,$c24,$c25,$c26,$c27,$c28,$c29,$c30,$c31,$c32,$c33,$c34,$c35,$c36,$c37,$c38,$c39,$c40,$c41,$c42,$c43,$c44,$c45,$c46,$c47,$c48,$c49,$c50,$c51,$c52,$c53,$c54,$c55,$c56,$c57,$c58,$c59,$c60,$c61,$c62,$c63,$c64,$c65,$c66,$c67,$c68,$c69,$c70,$c71,$c72,$c73,$c74,$c75,$c76,$c77,$c78,$c79,$c80,$c81,$c82,$c83,$c84,$c85,$c86,$c87,$c88,$c89,$c90,$c91,$c92,$c93,$c94,$c95,$c96,$c97,$c98,$c99)
```

```
SEARCH Products USING INDEX IX_Products_CategoryID (CategoryID=?)
```

