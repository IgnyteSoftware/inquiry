<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>
    <CheckEolTargetFramework>false</CheckEolTargetFramework>

    <!-- NuGet package metadata -->
    <RepositoryUrl>https://github.com/JakeOverstreet/inquiry</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageProjectUrl>https://github.com/JakeOverstreet/inquiry</PackageProjectUrl>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageTags>micro-orm;source-generator;ado.net;sql</PackageTags>
    <Authors>Inquiry Contributors</Authors>
    <Company>Inquiry Contributors</Company>
    <PackageReadmeFile>README.md</PackageReadmeFile>

    <!-- SourceLink: embed commit metadata so NuGet consumers can step into source -->
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>

    <!-- MinVer: version derived from nearest git tag (v8.0.0 → 8.0.0) -->
    <MinVerMinimumMajorMinor>8.0</MinVerMinimumMajorMinor>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.SourceLink.GitHub" PrivateAssets="all" />
    <PackageReference Include="MinVer" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup Condition="'$(IsPackable)' != 'false'">
    <None Include="$(MSBuildThisFileDirectory)README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
</Project>
